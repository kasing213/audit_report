node_modules/
telegram-session.json
# All StringSession files are full-account credentials — never commit any variant
# (company, -personal, .safe-bak, etc.).
telegram-string-session*
*.safe-bak
.env
*.log
debug-fail-*.png
debug-fail-*.html
